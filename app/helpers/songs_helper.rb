module SongsHelper
    # if were editing straight from non nested
    # resource, create this helper to allow us 
    # to select an artist from a drop down 
    # if were in the nested route under artists 
    # produce a hidden_field_tag so that we can send over 
    # artist_id params which would have existed from the 
    # controller - remember babysitting that value. 

end
